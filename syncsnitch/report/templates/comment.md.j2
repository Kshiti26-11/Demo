<!-- syncsnitch -->
### SyncSnitch found contract drift

**{{ summary.breaking }} breaking** of {{ summary.total }} changes · REST {{ breaking_by_surface.rest }} · gRPC {{ breaking_by_surface.grpc }} · DB {{ breaking_by_surface.db }} (run `{{ run_id }}`)

{% if breaking_changes %}
| Surface | Kind | Location | Old → New |
|---|---|---|---|
{% for c in breaking_changes %}
| {{ c.surface }} | {{ c.kind }} | `{{ c.location }}` | {{ c.old or "–" }} → {{ c.new or "–" }} |
{% endfor %}
{% endif %}

{% if affected_files %}
**Affected consumer files** ({{ candidates.summary.hits }} usages):

{% for f in affected_files %}
- `{{ f.file }}`{% if f.endpoints %} → {{ f.endpoints | join(", ") }}{% endif %}

{% endfor %}
{% endif %}
{% if candidates and candidates.summary.endpoints %}
**Affected endpoints:** {{ candidates.summary.endpoints | join(", ") }}
{% endif %}

Run /syncsnitch {{ upstream_pr_url or "<PR URL>" }} in IBM Bob to generate a verified companion PR.
